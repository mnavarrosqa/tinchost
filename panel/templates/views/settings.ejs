<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Settings – Tinchost Panel</title>
  <style>
    * { box-sizing: border-box; }
    body { font-family: system-ui, sans-serif; margin: 0; background: #f5f5f5; }
    .nav { background: #222; color: #fff; padding: 0.75rem 1.5rem; }
    .nav a { color: #fff; text-decoration: none; margin-right: 1rem; }
    main { max-width: 560px; margin: 0 auto; padding: 1.5rem; background: #fff; min-height: 80vh; }
    label { display: block; margin-top: 0.75rem; }
    input[type=text] { width: 100%; padding: 0.5rem; }
    button { padding: 0.5rem 1rem; background: #007bff; color: #fff; border: none; cursor: pointer; margin-top: 1rem; }
    .risk { background: #f8d7da; border-left: 4px solid #dc3545; padding: 0.75rem; margin: 1rem 0; }
  </style>
</head>
<body>
  <nav class="nav">
    <a href="/">Dashboard</a>
    <a href="/sites">Sites</a>
    <a href="/databases">Databases</a>
    <a href="/mail">Mail</a>
    <a href="/settings">Settings</a>
    <span style="float:right"><a href="/auth/logout">Logout</a></span>
  </nav>
  <main>
    <h1>Settings</h1>
    <div class="risk"><strong>This panel runs as root.</strong> Protect access and use a strong password.</div>
    <form method="post" action="/settings">
      <label>Nginx sites-available path</label>
      <input type="text" name="nginx_sites_available" value="<%= nginxSites %>">
      <label>PHP pool dir</label>
      <input type="text" name="php_pool_dir" value="<%= phpPoolDir %>">
      <label>Mail path (Maildir base)</label>
      <input type="text" name="mail_path" value="<%= mailPath %>">
      <label>MySQL root password (for creating DBs/users; leave blank to keep current)</label>
      <input type="password" name="mysql_root_password" value="<%= typeof mysqlRootPassword !== 'undefined' ? mysqlRootPassword : '' %>" placeholder="Set in wizard or here">
      <button type="submit">Save</button>
    </form>
    <p style="margin-top:2rem;">Wizard completed: <%= state && state.completed ? 'Yes' : 'No' %></p>
  </main>
</body>
</html>
