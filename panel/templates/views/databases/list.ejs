<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Databases – Tinchost Panel</title>
  <style>
    * { box-sizing: border-box; }
    body { font-family: system-ui, sans-serif; margin: 0; background: #f5f5f5; }
    .nav { background: #222; color: #fff; padding: 0.75rem 1.5rem; }
    .nav a { color: #fff; text-decoration: none; margin-right: 1rem; }
    main { max-width: 960px; margin: 0 auto; padding: 1.5rem; background: #fff; min-height: 80vh; }
    table { width: 100%; border-collapse: collapse; }
    th, td { padding: 0.5rem; text-align: left; border-bottom: 1px solid #ddd; }
    .card { border: 1px solid #ddd; padding: 1rem; margin: 1rem 0; }
    .btn { padding: 0.35rem 0.75rem; background: #007bff; color: #fff; border: none; cursor: pointer; border-radius: 4px; }
  </style>
</head>
<body>
  <nav class="nav">
    <a href="/">Dashboard</a>
    <a href="/sites">Sites</a>
    <a href="/databases">Databases</a>
    <a href="/mail">Mail</a>
    <a href="/settings">Settings</a>
    <span style="float:right"><a href="/auth/logout">Logout</a></span>
  </nav>
  <main>
    <h1>Databases</h1>
    <% if (typeof hasMysqlPassword !== 'undefined' && !hasMysqlPassword) { %>
    <p class="risk" style="background:#fff3cd;padding:0.75rem;">Set MySQL root password in <a href="/settings">Settings</a> so the panel can create databases and users on the server.</p>
    <% } %>
    <div class="card">
      <h2>Create database</h2>
      <form method="post" action="/databases/databases">
        <input type="text" name="name" placeholder="Database name" required>
        <button type="submit" class="btn">Create</button>
      </form>
    </div>
    <div class="card">
      <h2>Create user</h2>
      <form method="post" action="/databases/users">
        <input type="text" name="username" placeholder="Username" required>
        <input type="password" name="password" placeholder="Password">
        <input type="text" name="host" value="localhost" placeholder="Host">
        <button type="submit" class="btn">Create user</button>
      </form>
      <p class="risk" style="background:#fff3cd;padding:0.5rem;margin-top:0.5rem;"><strong>Risk:</strong> Using host <code>%</code> allows connections from any host; use localhost when possible.</p>
    </div>
    <h2>Databases</h2>
    <table>
      <thead><tr><th>Name</th></tr></thead>
      <tbody>
        <% databases.forEach(function(d) { %><tr><td><%= d.name %></td></tr><% }); %>
        <% if (!databases.length) { %><tr><td>None yet.</td></tr><% } %>
      </tbody>
    </table>
    <h2>Users</h2>
    <table>
      <thead><tr><th>Username</th><th>Host</th></tr></thead>
      <tbody>
        <% users.forEach(function(u) { %><tr><td><%= u.username %></td><td><%= u.host %></td></tr><% }); %>
        <% if (!users.length) { %><tr><td colspan="2">None yet.</td></tr><% } %>
      </tbody>
    </table>
  </main>
</body>
</html>
