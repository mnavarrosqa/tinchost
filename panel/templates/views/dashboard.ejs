<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard – Tinchost Panel</title>
  <style>
    * { box-sizing: border-box; }
    body { font-family: system-ui, sans-serif; margin: 0; background: #eef5ef; color: #1b4332; }
    .nav { background: #2d6a4f; color: #fff; padding: 0.75rem 1.5rem; display: flex; align-items: center; gap: 1rem; flex-wrap: wrap; }
    .nav a { color: #d8f3dc; text-decoration: none; display: inline-flex; align-items: center; gap: 0.4rem; }
    .nav a:hover { color: #fff; }
    .nav .nav-right { margin-left: auto; }
    .nav .ic { width: 18px; height: 18px; flex-shrink: 0; stroke: currentColor; fill: none; stroke-width: 2; stroke-linecap: round; stroke-linejoin: round; }
    .nav .nav-active { color: #fff; font-weight: 600; }
    main { max-width: 960px; margin: 0 auto; padding: 1.5rem; background: #fff; min-height: 80vh; border: 1px solid #b7d4bc; border-top: none; }
    h1 { margin-top: 0; color: #1b4332; font-size: 1.5rem; }
    .intro { color: #52796f; margin-bottom: 1.5rem; }
    .card { border: 1px solid #b7d4bc; border-radius: 6px; padding: 1.25rem; margin-bottom: 1rem; background: #fff; }
    .card h2 { margin: 0 0 0.75rem 0; color: #2d6a4f; font-size: 1.1rem; display: flex; align-items: center; gap: 0.5rem; }
    .card h2 .ic { width: 20px; height: 20px; color: #2d6a4f; stroke: currentColor; fill: none; stroke-width: 2; stroke-linecap: round; stroke-linejoin: round; }
    .card p { margin: 0.5rem 0; color: #1b4332; }
    .card a { color: #2d6a4f; font-weight: 500; text-decoration: none; display: inline-flex; align-items: center; gap: 0.35rem; }
    .card a:hover { color: #1b4332; text-decoration: underline; }
    .card a .ic { width: 16px; height: 16px; stroke: currentColor; fill: none; stroke-width: 2; stroke-linecap: round; stroke-linejoin: round; }
    .card ul { margin: 0; padding: 0; list-style: none; }
    .card li { padding: 0.35rem 0; border-bottom: 1px solid #e8f5e9; }
    .card li:last-child { border-bottom: none; }
    .server-meta { display: flex; flex-wrap: wrap; gap: 1rem 2rem; margin-bottom: 1rem; color: #52796f; font-size: 0.95rem; }
    .server-meta strong { color: #1b4332; }
    .metric { margin-bottom: 1rem; }
    .metric-label { display: flex; justify-content: space-between; align-items: baseline; margin-bottom: 0.35rem; font-size: 0.9rem; color: #1b4332; }
    .metric-label span:last-child { color: #52796f; font-size: 0.85rem; }
    .bar-track { height: 0.75rem; background: #e8f5e9; border-radius: 4px; overflow: hidden; }
    .bar-fill { height: 100%; border-radius: 4px; transition: width 0.2s ease; }
    .bar-fill.load { background: #2d6a4f; }
    .bar-fill.load.warn { background: #b7a53a; }
    .bar-fill.load.high { background: #9b2226; }
    .bar-fill.mem, .bar-fill.disk { background: #2d6a4f; }
    .bar-fill.mem.warn, .bar-fill.disk.warn { background: #b7a53a; }
    .bar-fill.mem.high, .bar-fill.disk.high { background: #9b2226; }
    .live-badge { font-size: 0.75rem; font-weight: normal; color: #52796f; margin-left: 0.35rem; }
    .live-badge::before { content: ''; display: inline-block; width: 0.4rem; height: 0.4rem; border-radius: 50%; background: #2d6a4f; margin-right: 0.25rem; vertical-align: middle; animation: pulse 2s ease-in-out infinite; }
    @keyframes pulse { 0%, 100% { opacity: 1; } 50% { opacity: 0.5; } }
  </style>
</head>
<body>
  <nav class="nav">
    <a href="/" class="nav-active"><svg class="ic" viewBox="0 0 24 24" aria-hidden="true"><path d="M3 9l9-7 9 7v11a2 2 0 01-2 2H5a2 2 0 01-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg>Dashboard</a>
    <a href="/wizard"><svg class="ic" viewBox="0 0 24 24" aria-hidden="true"><path d="M15 4V2m0 2v2m4-2v2m-2 4h2m2 4v2m0-4v-2m0 4h-2m-4 0h-2v2m0-4v-2m0 4H9m8-8h-2V4m0 2v2m0 8v2m0-4v-2m0 4h2M9 4H7v2h2V4zM5 20v-2H3v2h2zm0-4v-2H3v2h2z"/></svg>Wizard</a>
    <a href="/sites"><svg class="ic" viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="12" r="10"/><line x1="2" y1="12" x2="22" y2="12"/><path d="M12 2a15.3 15.3 0 014 10 15.3 15.3 0 01-4 10 15.3 15.3 0 01-4-10 15.3 15.3 0 014-10z"/></svg>Sites</a>
    <a href="/databases"><svg class="ic" viewBox="0 0 24 24" aria-hidden="true"><ellipse cx="12" cy="5" rx="9" ry="3"/><path d="M21 12c0 1.66-4 3-9 3s-9-1.34-9-3"/><path d="M3 5v14c0 1.66 4 3 9 3s9-1.34 9-3V5"/></svg>Databases</a>
    <a href="/mail"><svg class="ic" viewBox="0 0 24 24" aria-hidden="true"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/><polyline points="22,6 12,13 2,6"/></svg>Mail</a>
    <a href="/settings"><svg class="ic" viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 00.33 1.82l.06.06a2 2 0 010 2.83 2 2 0 01-2.83 0l-.06-.06a1.65 1.65 0 00-1.82-.33 1.65 1.65 0 00-1 1.51V21a2 2 0 01-2 2 2 2 0 01-2-2v-.09A1.65 1.65 0 009 19.4a1.65 1.65 0 00-1.82.33l-.06.06a2 2 0 01-2.83 0 2 2 0 010-2.83l.06-.06a1.65 1.65 0 00.33-1.82 1.65 1.65 0 00-1.51-1H3a2 2 0 01-2-2 2 2 0 012-2h.09A1.65 1.65 0 004.6 9a1.65 1.65 0 00-.33-1.82l-.06-.06a2 2 0 010-2.83 2 2 0 012.83 0l.06.06a1.65 1.65 0 001.82.33H9a1.65 1.65 0 001-1.51V3a2 2 0 012-2 2 2 0 012 2v.09a1.65 1.65 0 001 1.51 1.65 1.65 0 001.82-.33l.06-.06a2 2 0 012.83 0 2 2 0 010 2.83l-.06.06a1.65 1.65 0 00-.33 1.82V9a1.65 1.65 0 001.51 1H21a2 2 0 012 2 2 2 0 01-2 2h-.09a1.65 1.65 0 00-1.51 1z"/></svg>Settings</a>
    <span class="nav-right"><%= user.username %> · <a href="/auth/logout"><svg class="ic" viewBox="0 0 24 24" aria-hidden="true"><path d="M9 21H5a2 2 0 01-2-2V5a2 2 0 012-2h4"/><polyline points="16 17 21 12 16 7"/><line x1="21" y1="12" x2="9" y2="12"/></svg>Logout</a></span>
  </nav>
  <main>
    <h1>Hello, <%= user.username %></h1>
    <p class="intro">Tinchost Panel. Manage sites, databases, and mail from the menu.</p>
    <% if (typeof serverInfo !== 'undefined' && serverInfo) { %>
    <div class="card" id="server-card">
      <h2><svg class="ic" viewBox="0 0 24 24"><rect x="4" y="4" width="16" height="16" rx="2" ry="2"/><rect x="9" y="9" width="6" height="6"/><line x1="9" y1="1" x2="9" y2="4"/><line x1="15" y1="1" x2="15" y2="4"/><line x1="9" y1="20" x2="9" y2="23"/><line x1="15" y1="20" x2="15" y2="23"/><line x1="20" y1="9" x2="23" y2="9"/><line x1="20" y1="14" x2="23" y2="14"/><line x1="1" y1="9" x2="4" y2="9"/><line x1="1" y1="14" x2="4" y2="14"/></svg>Server <span class="live-badge" aria-live="polite">Live</span></h2>
      <div class="server-meta">
        <span><strong>Hostname</strong> <span id="server-hostname"><%= serverInfo.hostname %></span></span>
        <span><strong>IP</strong> <span id="server-ip"><%= serverInfo.mainIp %></span></span>
      </div>
      <div class="metric">
        <div class="metric-label"><span>Load average</span><span id="load-detail">1m: <%= serverInfo.load.one %> · 5m: <%= serverInfo.load.five %> · 15m: <%= serverInfo.load.fifteen %> (<%= serverInfo.load.cpus %> CPU<%= serverInfo.load.cpus !== 1 ? 's' : '' %>)</span></div>
        <div class="bar-track">
          <div id="load-bar" class="bar-fill load <%= serverInfo.load.onePerCpu >= 90 ? 'high' : (serverInfo.load.onePerCpu >= 70 ? 'warn' : '') %>" style="width: <%= Math.min(100, serverInfo.load.onePerCpu) %>%;"></div>
        </div>
      </div>
      <div class="metric">
        <div class="metric-label"><span>Memory</span><span id="memory-detail"><%= serverInfo.memory.usedFormatted %> of <%= serverInfo.memory.totalFormatted %> (<%= serverInfo.memory.usedPercent %>%)</span></div>
        <div class="bar-track">
          <div id="memory-bar" class="bar-fill mem <%= serverInfo.memory.usedPercent >= 90 ? 'high' : (serverInfo.memory.usedPercent >= 75 ? 'warn' : '') %>" style="width: <%= serverInfo.memory.usedPercent %>%;"></div>
        </div>
      </div>
      <div class="metric">
        <div class="metric-label"><span>Disk (root)</span><span id="disk-detail"><%= serverInfo.disk.usedFormatted %> of <%= serverInfo.disk.totalFormatted %> (<%= serverInfo.disk.usedPercent %>%)</span></div>
        <div class="bar-track">
          <div id="disk-bar" class="bar-fill disk <%= serverInfo.disk.usedPercent >= 90 ? 'high' : (serverInfo.disk.usedPercent >= 85 ? 'warn' : '') %>" style="width: <%= serverInfo.disk.usedPercent %>%;"></div>
        </div>
      </div>
    </div>
    <% } %>
    <div class="card">
      <h2><svg class="ic" viewBox="0 0 24 24"><polyline points="9 11 12 14 22 4"/><path d="M21 12v7a2 2 0 01-2 2H5a2 2 0 01-2-2V5a2 2 0 012-2h11"/></svg>Quick links</h2>
      <ul>
        <li><a href="/sites"><svg class="ic" viewBox="0 0 24 24"><circle cx="12" cy="12" r="10"/><line x1="2" y1="12" x2="22" y2="12"/><path d="M12 2a15.3 15.3 0 014 10 15.3 15.3 0 01-4 10 15.3 15.3 0 01-4-10 15.3 15.3 0 014-10z"/></svg>Sites</a> – Add or edit websites (Nginx, PHP version, SSL, FTP).</li>
        <li><a href="/databases"><svg class="ic" viewBox="0 0 24 24"><ellipse cx="12" cy="5" rx="9" ry="3"/><path d="M21 12c0 1.66-4 3-9 3s-9-1.34-9-3"/><path d="M3 5v14c0 1.66 4 3 9 3s9-1.34 9-3V5"/></svg>Databases</a> – Create MySQL/MariaDB databases and users.</li>
        <li><a href="/mail"><svg class="ic" viewBox="0 0 24 24"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/><polyline points="22,6 12,13 2,6"/></svg>Mail</a> – Mail domains and mailboxes.</li>
        <li><a href="/settings"><svg class="ic" viewBox="0 0 24 24"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 00.33 1.82l.06.06a2 2 0 010 2.83 2 2 0 01-2.83 0l-.06-.06a1.65 1.65 0 00-1.82-.33 1.65 1.65 0 00-1 1.51V21a2 2 0 01-2 2 2 2 0 01-2-2v-.09A1.65 1.65 0 009 19.4a1.65 1.65 0 00-1.82.33l-.06.06a2 2 0 01-2.83 0 2 2 0 010-2.83l.06-.06a1.65 1.65 0 00.33-1.82 1.65 1.65 0 00-1.51-1H3a2 2 0 01-2-2 2 2 0 012-2h.09A1.65 1.65 0 004.6 9a1.65 1.65 0 00-.33-1.82l-.06-.06a2 2 0 010-2.83 2 2 0 012.83 0l.06.06a1.65 1.65 0 001.82.33H9a1.65 1.65 0 001-1.51V3a2 2 0 012-2 2 2 0 012 2v.09a1.65 1.65 0 001 1.51 1.65 1.65 0 001.82-.33l.06-.06a2 2 0 012.83 0 2 2 0 010 2.83l-.06.06a1.65 1.65 0 00-.33 1.82V9a1.65 1.65 0 001.51 1H21a2 2 0 012 2 2 2 0 01-2 2h-.09a1.65 1.65 0 00-1.51 1z"/></svg>Settings</a> – Paths, MySQL password, and installed services (PHP, database, FTP, mail).</li>
      </ul>
    </div>
  </main>
  <% if (typeof serverInfo !== 'undefined' && serverInfo) { %>
  <script>
  (function() {
    var card = document.getElementById('server-card');
    if (!card) return;
    var loadDetail = document.getElementById('load-detail');
    var loadBar = document.getElementById('load-bar');
    var memoryDetail = document.getElementById('memory-detail');
    var memoryBar = document.getElementById('memory-bar');
    var diskDetail = document.getElementById('disk-detail');
    var diskBar = document.getElementById('disk-bar');
    var hostnameEl = document.getElementById('server-hostname');
    var ipEl = document.getElementById('server-ip');
    function barClass(type, pct) {
      if (type === 'load') return pct >= 90 ? 'high' : (pct >= 70 ? 'warn' : '');
      if (type === 'mem') return pct >= 90 ? 'high' : (pct >= 75 ? 'warn' : '');
      return pct >= 90 ? 'high' : (pct >= 85 ? 'warn' : '');
    }
    function update(data) {
      if (!data) return;
      var cpuLabel = data.load.cpus === 1 ? 'CPU' : 'CPUs';
      if (loadDetail) loadDetail.textContent = '1m: ' + data.load.one + ' · 5m: ' + data.load.five + ' · 15m: ' + data.load.fifteen + ' (' + data.load.cpus + ' ' + cpuLabel + ')';
      if (loadBar) {
        var lw = Math.min(100, data.load.onePerCpu);
        loadBar.style.width = lw + '%';
        loadBar.className = 'bar-fill load ' + barClass('load', data.load.onePerCpu);
      }
      if (memoryDetail) memoryDetail.textContent = data.memory.usedFormatted + ' of ' + data.memory.totalFormatted + ' (' + data.memory.usedPercent + '%)';
      if (memoryBar) {
        memoryBar.style.width = data.memory.usedPercent + '%';
        memoryBar.className = 'bar-fill mem ' + barClass('mem', data.memory.usedPercent);
      }
      if (diskDetail) diskDetail.textContent = data.disk.usedFormatted + ' of ' + data.disk.totalFormatted + ' (' + data.disk.usedPercent + '%)';
      if (diskBar) {
        diskBar.style.width = data.disk.usedPercent + '%';
        diskBar.className = 'bar-fill disk ' + barClass('disk', data.disk.usedPercent);
      }
      if (hostnameEl) hostnameEl.textContent = data.hostname;
      if (ipEl) ipEl.textContent = data.mainIp;
    }
    function fetchServerInfo() {
      fetch('/api/server-info').then(function(r) { return r.ok ? r.json() : null; }).then(update).catch(function() {});
    }
    setInterval(fetchServerInfo, 4000);
    setTimeout(fetchServerInfo, 1000);
  })();
  </script>
  <% } %>
</body>
</html>
